# Sanity: create a project at https://www.sanity.io/manage, then set:
SANITY_PROJECT_ID=your_project_id
SANITY_DATASET=production
# Optional: create a read token in API → Tokens for front-end fetching
SANITY_READ_TOKEN=
